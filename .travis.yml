language: python
python:
  - "2.7"

# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
  - pip install pylint

# command to run tests
script: pytest server.js

